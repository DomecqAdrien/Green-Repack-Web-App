export class Article {
    id: number;
    nom: string;
    prix: string;

    constructor(id: number, nom: string, prix: string){
        this.id = id;
        this.nom = nom;
        this.prix = prix;
    }
}
